{
  "name": "nodejs-cloud-function-trigger-cloud-build",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project .",
    "start": "yarn build && functions-framework --target=triggerCloudBuild --source dist"
  },
  "dependencies": {
    "@google-cloud/cloudbuild": "^2.0.6"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.7.1",
    "@types/express": "^4.17.11",
    "typescript": "^4.1.3"
  }
}
